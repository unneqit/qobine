# Connect support

Initial support for Qobuz Connect.

Can be run as a standalone player, or enabled in web, rfid and tui with the `--connect` flag.

## Limitations
Currently only a subset of Connect is implemented.
Only commands where an official player controls the player is supported, and still only a subset.
